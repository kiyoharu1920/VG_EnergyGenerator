# Bun Runtime

This branch keeps the same Next.js app source and runs the existing package scripts through Bun.

## Commands

```powershell
bun install
bun run dev
bun run build
bun run start
bun run lint
```

Use this branch when comparing Bun startup/build behavior against the Deno branch.
